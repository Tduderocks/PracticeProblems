
#doesnt work
""" program that counts the lines and chars in a file"""
def countlines(name):
    """reads an input file and counts the number of lines"""
    print(len(name.readlines))
def countChars(name):
    """reads an input file and counts the number of chars in it"""
    print(len(read(name)))
def test(name):
    """calls functions with a given input file name"""
    print('the file has', countlines(count_lines.txt), 'lines')
    print('the file has', countChars(count_lines.txt), 'characters')


